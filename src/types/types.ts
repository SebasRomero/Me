export type bullets = {
    bullet1: string
    bullet2: string
    bullet3: string
}

export type experience = {
    bullets: bullets
    position: string
    company: string
    date: string
    tools: string[]
}